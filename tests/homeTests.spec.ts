import { test, expect } from '@playwright/test';
import { HomePage } from './Pages/HomePage';

test.describe('A. Home Page (Product List) tests', () => {
    test('Verify Product Table Loading', async ({ page }) => {
        
    })
    
    
})
